# Memory-to-Memory Copy

ѕусть в задаче n потоков. —оздадим два массива one и zero длин n. ѕри вызове Decide потоком #i будем делать следующее:
1. Mem-Copy(one[i], zero[i])
2. ѕройдЄмс€ по массиву one и рекурсивно будем записывать нолики в каждый регистр.  “ем самым мы прекратим копирование единичек в массив zero и обеспечим zero его статичность (это позволит выполнить свойство `agreement`).
3. “еперь, так как изменений в zero больше не происходит, пройдЄмс€ по массиву и объ€вим победителем поток с номером, соответствующим первой попавшейс€ единичке (эту единичку установил один из потоков, поэтому свойство `validity` выполн€етс€). “акой поток точно найдЄтс€, потому что как минимум поток i записывал единичку.

Ќаш алгопитм работает за константное врем€ и заканчиваетс€ вне зависимости от результата исполнени€ другого потока, поэтому свойство `wait-free termination` тоже выполнено.
